kind: Module
description: Image for the API backend that sends email/sms
type: container
name: api-hermes-image
repositoryUrl: https://github.com/AlpacaLabs/api-hermes.git#master
buildArgs:
  SSH_PRIVATE_KEY: ${var.ssh_private_key}